# Copy this file to .env.local and fill in values.
# Rename: cp env.example.txt .env.local

# Supabase — create project at supabase.com, then fill in:
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Step-2 GUI display gate (lib/compliance/step1.ts) — demo display flag, not a
# secret. Set to 1 so a plain `npm run build && npm start` shows the step-2
# compliance GUI (scorecard, detail findings, map legend).
NEXT_PUBLIC_SO_SHOW_COMPLIANCE=1

# E2E test hooks (window.__soStreetColors, VLA-13) — diagnostics-only
# global read by tests/e2e/street-coverage.e2e.mjs and
# tests/e2e/vla21-shared-municipality-areas.e2e.mjs. Set to 1 for local
# dev/E2E builds only. MUST stay unset in the real Vercel prod environment.
NEXT_PUBLIC_SO_E2E_HOOKS=1

# Google Routes API (for P-c transit illustration — requires Routes API enabled)
# See docs/GOOGLE_API_SETUP.md
GOOGLE_MAPS_API_KEY=your-google-maps-api-key

# Routing service (OSRM or Valhalla — see routing/README.md)
ROUTING_URL=http://localhost:5000

# OpenRouter (LLM — optional, for P-e non-authoritative summary only)
OPENROUTER_API_KEY=your-openrouter-api-key
OPENROUTER_MODEL=openai/gpt-4o-mini
